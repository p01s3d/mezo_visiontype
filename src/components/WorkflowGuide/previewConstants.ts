/** Text column width — matches standard preview width so layout stays even when preview narrows. */
export const GUIDE_TEXT_COLUMN_WIDTH = 492;

/** Right-column width at guide max layout (1080px container, 32px padding, 64px gap). */
export const GUIDE_PREVIEW_WIDTH = GUIDE_TEXT_COLUMN_WIDTH;

/** Narrow preview column — trade rail and similar vertical panels. */
export const GUIDE_PREVIEW_WIDTH_NARROW = 360;

/** Uniform min height for every guide section row (text + preview columns). */
export const GUIDE_SECTION_HEIGHT = 480;

/** @deprecated Use GUIDE_SECTION_HEIGHT */
export const GUIDE_PREVIEW_HEIGHT = GUIDE_SECTION_HEIGHT;
